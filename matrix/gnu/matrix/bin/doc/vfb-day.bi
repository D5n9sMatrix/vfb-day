#inclib "vfb-day"
Declare Function NewOrder( ByVal x As Integer, ByVal y As Integer ) As Integer
type DayPerfect
   Dim DayPerfectTypes As boolean
   Dim DayPerfectEvent As boolean
   Dim DayPerfectObjes As boolean
end type
Dim Credit As Integer
Credit=2800
Assert(Credit=2800)
End

