#inclib "vfb-day"
' my responsibility is to make it execute the headers 
' of all elements on the run and on the run.
Declare Function WinExecHeaders (ByRef All As boolean)
type AllRun
   Dim AllRunTypes As DayPerfect
   Dim AllRunEvent As DayPerfect
   Dim AllRunObjes As DayPerfect
end type
End
